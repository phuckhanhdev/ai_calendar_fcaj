import connectToDatabase from "./connection.js";

// File này dùng làm nơi cấu hình các giá trị mặc định cho database sau này
console.log("🌱 [Seed] Bắt đầu gieo mầm dữ liệu (seed data)...");
console.log("🌱 [Seed] Hiện tại chưa có dữ liệu mẫu bắt buộc. Seed hoàn tất!");
process.exit(0);
